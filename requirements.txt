# X (Twitter) Affiliate Bot - Dependencies
# All libraries use free-tier APIs only

# Google Gemini API (text generation)
google-generativeai>=0.7.0

# X (Twitter) API client (supports v1.1 media upload + v2 posting)
tweepy>=4.14.0

# HTTP requests (Hugging Face API, RSS fetching)
requests>=2.31.0

# RSS/Atom feed parsing (Google Trends RSS)
feedparser>=6.0.11

# Image handling (convert HuggingFace response to bytes)
Pillow>=10.0.0

# Environment variable management
python-dotenv>=1.0.0

# HTML parsing (for scraping trend keywords if needed)
beautifulsoup4>=4.12.0

# Rate limiting / retry utilities
tenacity>=8.2.0
